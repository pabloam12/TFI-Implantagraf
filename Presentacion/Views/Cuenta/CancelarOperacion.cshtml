@Html.Partial("_Espacio1")


<body>

    <div class="container contact_container">
        <div>
            @if ((String)Session["IdiomaApp"] == "Esp" || (String)Session["IdiomaApp"] == null)
            {
                <div class="alert alert-success" role="alert">SE HA CANCELADO LA OPERACIÓN CON ÉXITO.</div>
            }
            else
            {

                <div class="alert alert-success" role="alert">THE OPERATION HAS BEEN SUCCESSFULLY CANCELLED.</div>
            }

        </div>

        @if ((String)Session["IdiomaApp"] == "Esp" || (String)Session["IdiomaApp"] == null)
        {

            <div class="form-group">
                @Html.ActionLink("VOLVER", "Index", "Home", null, new { @class = "btn btn-info btn-sm" })
            </div>
        }
        else
        { <div class="form-group">
            @Html.ActionLink("GO BACK", "Index", "Home", null, new { @class = "btn btn-info btn-sm" })
        </div>
        }
    </div>
</body>


@Html.Partial("_Espacio2")