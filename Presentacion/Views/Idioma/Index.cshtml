@model IEnumerable<Entidades.Idioma>

@{
    ViewBag.Title = "Idiomas";
    
}

@Html.Partial("_Espacio1")

<h2>Idiomas</h2>
<br />

<p>
    @Html.ActionLink("Nuevo Idioma", "Crear", null, new { @class = "btn btn-primary btn-sm" })
</p>
<br />

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Abreviacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Abreviacion)
        </td>
        @*<td>
            @Html.ActionLink("Editar", "Editar", new { id=item.Id }) |
            @Html.ActionLink("Borrar", "Borrar", new { id=item.Id })
        </td>*@
    </tr>
}

</table>

<div class="row">
    @Html.ActionLink("Volver", "Index", "Home")
</div>

@Html.Partial("_Espacio2")
